# E-commerce Clothing Website (CRA + JSX)

## Run
```bash
npm install
npm start
```

## Notes
- This project was converted from a Figma-generated Vite/TypeScript codebase to Create React App + plain JSX.
- If you see broken images, replace any `figma:asset/...` references with real files under `src/assets/` and update the import.
